#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int n;
    printf("Ingrese el numero de casillas: ");
    scanf("%d", &n);
    int v[n];
    for (int i = 0; i < n; ++i)
     {
        printf("Ingrese el numero: ");
    scanf("%d", &v[i]);
    }
    for (int i = 0; i < n; ++i)
     {
        printf("los numeros son: %d ", v[i]);
    }
    return 0;
}
